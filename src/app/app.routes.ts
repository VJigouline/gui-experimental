import { Routes } from '@angular/router';
import { MainPage } from './components/gui/main-page/main-page';

export const routes: Routes = [
    {
        path: '',
        component: MainPage,
    }
];
